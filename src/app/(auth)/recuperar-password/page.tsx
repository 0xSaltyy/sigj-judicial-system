import { recoverPassword } from "@/app/actions/auth";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Alert, AlertDescription } from "@/components/ui/alert";
export const metadata = { title: "Recuperar contraseña" };
export default async function RecoverPage({ searchParams }: { searchParams: Promise<{ error?: string; sent?: string }> }) { const query = await searchParams; return <><h1 className="text-3xl font-semibold text-[#102d49]">Restablecimiento manual</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">Por seguridad no se envían enlaces de acceso ni correos de recuperación. Solicite al OWNER una contraseña temporal nueva.</p>{query.sent && <Alert className="mt-5 border-blue-200 bg-blue-50"><AlertDescription>Solicitud registrada. Si la cuenta existe, el OWNER podrá restablecerla manualmente desde el panel interno.</AlertDescription></Alert>}{query.error && <Alert variant="destructive" className="mt-5"><AlertDescription>{query.error}</AlertDescription></Alert>}<form action={recoverPassword} className="mt-7 space-y-4"><div className="space-y-2"><Label htmlFor="email">Correo de autenticación</Label><Input id="email" name="email" type="email" required className="h-11 rounded-none" /></div><Button className="h-11 w-full rounded-none bg-[#153b5c]">Solicitar restablecimiento manual</Button></form></>; }
