import Link from "next/link";
import { ArrowRight, Calendar, Megaphone } from "lucide-react";
import { PageHero } from "@/components/page-hero";
import { notices, formatDate } from "@/lib/demo-data";
import { Badge } from "@/components/ui/badge";
export const metadata = { title: "Comunicados" };
export default function NoticesPage() { return <><PageHero title="Comunicados institucionales" description="Avisos, novedades y comunicaciones emitidas por las dependencias del Departamento." /><div className="mx-auto max-w-[1180px] px-4 py-12 sm:px-6 lg:px-8 lg:py-16"><div className="mb-8 flex items-center gap-2 text-sm text-muted-foreground"><Megaphone className="size-4" /> {notices.length} publicaciones vigentes</div><div className="divide-y border bg-white">{notices.map((item) => <article key={item.slug} className="group p-6 transition hover:bg-[#edf5fb]"><div className="flex flex-wrap items-center justify-between gap-3"><Badge variant="outline" className="rounded-none border-[#b21b1b] bg-white text-[#b21b1b]">{item.category}</Badge><time className="flex items-center gap-1.5 text-xs text-muted-foreground"><Calendar className="size-3.5" />{formatDate(item.date)}</time></div><h2 className="mt-5 font-serif text-2xl font-semibold leading-8 text-[#143654]">{item.title}</h2><p className="mt-3 max-w-3xl text-sm leading-6 text-muted-foreground">{item.excerpt}</p><div className="mt-5"><p className="text-xs text-slate-500">{item.entity}</p><Link href={`/comunicados/${item.slug}`} className="mt-3 flex items-center gap-2 text-sm font-semibold text-[#005ea8]">Leer comunicado <ArrowRight className="size-4 transition group-hover:translate-x-1" /></Link></div></article>)}</div></div></>; }
