import Link from "next/link";
import { ArrowRight, Calendar, Megaphone } from "lucide-react";
import { PageHero } from "@/components/page-hero";
import { notices, formatDate } from "@/lib/demo-data";
import { Badge } from "@/components/ui/badge";
export const metadata = { title: "Comunicados" };
export default function NoticesPage() { return <><PageHero title="Comunicados institucionales" description="Avisos, novedades y comunicaciones emitidas por las dependencias del sistema demostrativo." /><div className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8 lg:py-16"><div className="mb-8 flex items-center gap-2 text-sm text-muted-foreground"><Megaphone className="size-4" /> {notices.length} publicaciones vigentes</div><div className="grid gap-5 lg:grid-cols-3">{notices.map((item) => <article key={item.slug} className="group flex min-h-72 flex-col rounded-lg border bg-white p-6 transition hover:border-[#c0a15a] hover:shadow-lg"><div className="flex items-center justify-between"><Badge variant="outline" className="border-[#d8c38f] bg-[#fbf7ed] text-[#795c25]">{item.category}</Badge><time className="flex items-center gap-1.5 text-xs text-muted-foreground"><Calendar className="size-3.5" />{formatDate(item.date)}</time></div><h2 className="mt-6 text-xl font-semibold leading-7 text-[#143654]">{item.title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{item.excerpt}</p><div className="mt-auto pt-6"><p className="text-xs text-slate-500">{item.entity}</p><Link href={`/comunicados/${item.slug}`} className="mt-3 flex items-center gap-2 text-sm font-semibold text-[#8b6829]">Leer comunicado <ArrowRight className="size-4 transition group-hover:translate-x-1" /></Link></div></article>)}</div></div></>; }
