import { notFound } from "next/navigation";
import { notices, formatDate } from "@/lib/demo-data";
import { Badge } from "@/components/ui/badge";
import { PrintButton } from "@/components/print-button";
export default async function NoticeDetail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const item = notices.find((n) => n.slug === slug); if (!item) notFound(); return <div className="mx-auto max-w-4xl px-4 py-12 sm:px-6 lg:py-16"><article className="paper rounded-lg border p-7 sm:p-12"><div className="flex flex-wrap items-center justify-between gap-4"><Badge variant="outline" className="border-[#d8c38f] bg-[#fbf7ed] text-[#795c25]">{item.category}</Badge><PrintButton /></div><h1 className="mt-8 text-3xl font-semibold leading-tight text-[#102d49] sm:text-4xl">{item.title}</h1><div className="mt-5 border-y py-4 text-sm text-muted-foreground">{item.entity} · {formatDate(item.date)}</div><div className="mt-8 space-y-5 text-[15px] leading-8 text-slate-700"><p>{item.excerpt}</p><p>El Tribunal Superior de Justicia de la República Judicial informa a todas las personas usuarias que esta comunicación hace parte de un entorno estrictamente demostrativo.</p><h2 className="text-lg font-semibold text-[#153553]">Información para usuarios</h2><p>Los canales digitales permanecerán disponibles para consulta pública. Las actuaciones incluidas en el sistema carecen de efectos jurídicos y no sustituyen ningún canal oficial.</p><p className="border-l-2 border-[#b38a3c] bg-[#fbf8f0] p-4 text-sm">Para efectos de esta demostración, no remita información personal, documentos reales ni datos sujetos a reserva.</p></div></article></div>; }
