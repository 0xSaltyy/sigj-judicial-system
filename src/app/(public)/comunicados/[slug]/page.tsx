import { notFound } from "next/navigation";
import { notices, formatDate } from "@/lib/demo-data";
import { Badge } from "@/components/ui/badge";
import { PrintButton } from "@/components/print-button";
export default async function NoticeDetail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const item = notices.find((n) => n.slug === slug); if (!item) notFound(); return <div className="mx-auto max-w-4xl px-4 py-12 sm:px-6 lg:py-16"><article className="border bg-white p-7 sm:p-12"><div className="flex flex-wrap items-center justify-between gap-4"><Badge variant="outline" className="rounded-none border-[#b21b1b] bg-white text-[#b21b1b]">{item.category}</Badge><PrintButton /></div><h1 className="mt-8 font-serif text-3xl font-semibold leading-tight text-[#102d49] sm:text-4xl">{item.title}</h1><div className="mt-5 border-y py-4 text-sm text-muted-foreground">{item.entity} · {formatDate(item.date)}</div><div className="mt-8 space-y-5 text-[15px] leading-8 text-slate-700"><p>{item.excerpt}</p><p>El Departamento informa a su comunidad que los canales digitales permanecen disponibles para consulta pública, radicación interna y seguimiento de publicaciones.</p><h2 className="font-serif text-lg font-semibold text-[#153553]">Información para usuarios</h2><p>Las actuaciones incluidas en el sistema se organizan mediante controles de acceso, auditoría y trazabilidad para mantener un registro institucional ordenado.</p></div></article></div>; }
