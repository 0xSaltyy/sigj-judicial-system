import { notFound } from "next/navigation";
import { proceedings } from "@/lib/demo-data";
import { PrintButton } from "@/components/print-button";
export default async function ProceedingDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const item = proceedings.find((p) => p.id === id); if (!item) notFound(); return <div className="mx-auto max-w-4xl px-4 py-12 sm:px-6"><div className="mb-5 flex justify-end"><PrintButton label="Imprimir providencia" /></div><article className="paper min-h-[1000px] border px-8 py-12 sm:px-16"><header className="border-b-2 border-[#153553] pb-7 text-center"><p className="text-xs font-semibold uppercase tracking-[.18em] text-[#98712b]">Department of Justice Roleplay</p><h1 className="mt-3 font-serif text-xl font-bold uppercase text-[#102d49]">{item.title}</h1><p className="mono-number mt-2 text-sm">{item.number}</p><p className="mt-3 text-[10px] font-bold uppercase text-red-800">ROLEPLAY DOCUMENT — NOT A REAL GOVERNMENT OR COURT ORDER.</p></header><dl className="my-8 grid gap-2 text-sm sm:grid-cols-[160px_1fr]"><dt className="font-semibold">Docket</dt><dd className="mono-number">{item.caseNumber}</dd><dt className="font-semibold">División</dt><dd>{item.chamber}</dd><dt className="font-semibold">Funcionario</dt><dd>{item.judge}</dd></dl><div className="space-y-7 text-justify text-sm leading-7 text-slate-800"><section><h2 className="font-bold uppercase">I. Antecedentes ficticios</h2><p className="mt-2">La división examina el asunto de roleplay identificado en la referencia para ordenar su continuidad narrativa.</p></section><section><h2 className="font-bold uppercase">II. Consideraciones</h2><p className="mt-2">Verificada la competencia simulada y la oportunidad de la actuación, se advierte que concurren los presupuestos internos para continuar con el trámite ficticio. Ninguna mención de este documento corresponde a un proceso gubernamental real.</p></section><section><h2 className="font-bold uppercase">III. Resuelve</h2><p className="mt-2"><strong>PRIMERO.</strong> DISPONER la continuación del trámite roleplay en los términos expuestos.</p><p><strong>SEGUNDO.</strong> Comuníquese esta decisión ficticia a los participantes de la comunidad.</p></section><p className="pt-8 text-center font-semibold">Publíquese para fines de roleplay.</p><div className="mx-auto mt-16 w-64 border-t pt-2 text-center text-xs"><p className="font-semibold">Firma electrónica simulada</p><p className="text-muted-foreground">{item.judge}</p></div></div><footer className="mt-20 border-t pt-4 text-center text-[10px] text-muted-foreground">Documento ficticio · Sin validez jurídica real · Developed by: kcobainn</footer></article></div>; }
