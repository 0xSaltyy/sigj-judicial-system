import Link from "next/link";
import { ArrowRight, BookOpen, Filter } from "lucide-react";
import { PageHero } from "@/components/page-hero";
import { proceedings, formatDate } from "@/lib/demo-data";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
export const metadata = { title: "Biblioteca de providencias" };
export default function ProceedingsPage() { return <><PageHero eyebrow="Relatoría" title="Biblioteca de providencias" description="Autos y sentencias ficticias publicados para consulta académica." /><div className="mx-auto max-w-6xl px-4 py-12 sm:px-6 lg:px-8 lg:py-16"><div className="mb-6 flex flex-col gap-3 rounded-lg border bg-white p-4 sm:flex-row"><Input placeholder="Buscar por radicado, título o ponente…" /><Button variant="outline" className="gap-2"><Filter className="size-4" /> Filtros</Button></div><div className="divide-y rounded-lg border bg-white">{proceedings.map((item) => <article key={item.id} className="group flex flex-col gap-5 p-6 sm:flex-row"><div className="grid size-11 shrink-0 place-items-center rounded bg-[#edf2f6] text-[#183d61]"><BookOpen className="size-5" /></div><div className="flex-1"><p className="text-[11px] font-semibold uppercase tracking-wider text-[#98712b]">{item.type} · {item.chamber}</p><h2 className="mt-2 text-lg font-semibold text-[#153553]">{item.title}</h2><p className="mono-number mt-2 text-xs text-muted-foreground">{item.number} · {item.caseNumber}</p><p className="mt-2 text-xs text-slate-500">{item.judge} · {formatDate(item.date)}</p></div><Link href={`/providencias/${item.id}`} className="flex items-center gap-2 self-start text-sm font-semibold text-[#8b6829] sm:self-center">Consultar <ArrowRight className="size-4 transition group-hover:translate-x-1" /></Link></article>)}</div></div></>; }
