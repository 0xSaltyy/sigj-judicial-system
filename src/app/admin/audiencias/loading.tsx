import { AdminRouteLoading } from "@/components/loading-states";

export default function HearingsLoading() {
  return <AdminRouteLoading title="Cargando audiencias…" detail="Preparando calendario, filtros y próximos eventos." />;
}
