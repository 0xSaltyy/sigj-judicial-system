import { CalendarPlus, MoreHorizontal } from "lucide-react";
import { AdminPageHeader } from "@/components/admin-page";
import { Button } from "@/components/ui/button";
import { CaseStatusBadge } from "@/components/status-badges";
import { hearings } from "@/lib/demo-data";
export default function AdminHearingsPage() { return <><AdminPageHeader title="Agenda de audiencias" description="Programación y gestión de sesiones físicas y virtuales." action={<Button className="gap-2 bg-[#153b5c]"><CalendarPlus className="size-4" /> Programar audiencia</Button>} /><div className="grid gap-4 lg:grid-cols-2">{hearings.map((item) => <article key={item.id} className="rounded-lg border bg-white p-5"><div className="flex items-start justify-between"><div className="flex gap-4"><div className="rounded bg-[#153b5c] px-3 py-2 text-center text-white"><p className="text-xs font-semibold">{item.date}</p><p className="mono-number mt-1 text-sm text-[#dfc985]">{item.time}</p></div><div><p className="text-xs font-semibold uppercase tracking-wider text-[#9a752f]">{item.type}</p><h2 className="mt-1 text-base font-semibold text-[#153553]">{item.title}</h2><p className="mono-number mt-1 text-xs text-muted-foreground">{item.caseNumber}</p></div></div><Button variant="ghost" size="icon"><MoreHorizontal className="size-4" /></Button></div><div className="mt-5 flex flex-wrap items-center justify-between gap-3 border-t pt-4"><p className="text-xs text-muted-foreground">{item.court} · {item.room} · {item.public ? "Pública" : "Reservada"}</p><CaseStatusBadge status={item.status} /></div></article>)}</div></>; }
