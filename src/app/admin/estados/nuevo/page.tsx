import { createJudicialState } from "@/app/actions/states"; import { AdminPageHeader } from "@/components/admin-page"; import { ActionMessage } from "@/components/action-message"; import { SubmitButton } from "@/components/submit-button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Textarea } from "@/components/ui/textarea"; import { requirePermission,RESOURCE_ROLES } from "@/lib/auth/permissions";
export default async function NewState({searchParams}:{searchParams:Promise<{error?:string}>}){const [{supabase},q]=await Promise.all([requirePermission(RESOURCE_ROLES.secretarialWrite),searchParams]);const [{data:dependencies},{data:cases}]=await Promise.all([supabase.from("dependencies").select("id,name").eq("is_active",true).order("name"),supabase.from("cases").select("id,internal_number").order("filed_at",{ascending:false}).limit(100)]);return <><AdminPageHeader title="Crear estado judicial" description="Incluya una actuación y publique cuando corresponda."/><ActionMessage error={q.error}/><form action={createJudicialState} className="grid max-w-3xl gap-5 rounded-lg border bg-white p-6"><Field label="Dependencia"><select name="dependency_id" required className="h-9 rounded-md border px-3"><option value="">Seleccione…</option>{(dependencies??[]).map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select></Field><Field label="Fecha"><Input name="state_date" type="date" required defaultValue={new Date().toISOString().slice(0,10)}/></Field><Field label="Expediente"><select name="case_id" required className="h-9 rounded-md border px-3"><option value="">Seleccione…</option>{(cases??[]).map(c=><option key={c.id} value={c.id}>{c.internal_number}</option>)}</select></Field><Field label="Descripción"><Textarea name="description" required/></Field><Field label="Estado"><select name="status" className="h-9 rounded-md border px-3"><option>Borrador</option><option>Publicado</option></select></Field><SubmitButton pendingLabel="Creando…">Crear estado</SubmitButton></form></>};function Field({label,children}:{label:string;children:React.ReactNode}){return <div className="grid gap-2"><Label>{label}</Label>{children}</div>}
