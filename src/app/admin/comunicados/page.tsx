import { MoreHorizontal, Plus } from "lucide-react";
import { AdminPageHeader } from "@/components/admin-page";
import { Button } from "@/components/ui/button";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { notices, formatDate } from "@/lib/demo-data";
import { CaseStatusBadge } from "@/components/status-badges";
export default function AdminNoticesPage() { return <><AdminPageHeader title="Comunicados" description="Gestión editorial de información institucional y avisos públicos." action={<Button className="gap-2 bg-[#153b5c]"><Plus className="size-4" /> Nuevo comunicado</Button>} /><div className="overflow-hidden rounded-lg border bg-white"><Table><TableHeader><TableRow className="bg-slate-50"><TableHead>Título</TableHead><TableHead>Categoría</TableHead><TableHead>Entidad emisora</TableHead><TableHead>Publicación</TableHead><TableHead>Estado</TableHead><TableHead /></TableRow></TableHeader><TableBody>{notices.map((item) => <TableRow key={item.slug}><TableCell className="max-w-md"><p className="text-sm font-semibold text-[#153553]">{item.title}</p><p className="mt-1 text-xs text-muted-foreground">/{item.slug}</p></TableCell><TableCell className="text-xs">{item.category}</TableCell><TableCell className="text-xs">{item.entity}</TableCell><TableCell className="text-xs">{formatDate(item.date)}</TableCell><TableCell><CaseStatusBadge status="Publicado" /></TableCell><TableCell><Button variant="ghost" size="icon"><MoreHorizontal className="size-4" /></Button></TableCell></TableRow>)}</TableBody></Table></div></>; }
