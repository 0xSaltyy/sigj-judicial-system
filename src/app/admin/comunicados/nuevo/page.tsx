import {AdminPageHeader} from "@/components/admin-page";import {ActionMessage} from "@/components/action-message";import {NoticeForm} from "@/components/notice-form";import {requirePermission,RESOURCE_ROLES} from "@/lib/auth/permissions";export default async function NewNotice({searchParams}:{searchParams:Promise<{error?:string}>}){const [,q]=await Promise.all([requirePermission(RESOURCE_ROLES.noticesWrite),searchParams]);return <><AdminPageHeader title="Nuevo comunicado" description="Redacte, previsualice y publique."/><ActionMessage error={q.error}/><NoticeForm/></>}
