import { History } from "lucide-react";
export function AuditTimeline({ entries }: { entries: { id: string; action: string; description: string; date: string }[] }) { return <ol className="space-y-4">{entries.map((entry) => <li key={entry.id} className="flex gap-3"><span className="grid size-8 shrink-0 place-items-center rounded-full bg-slate-100 text-slate-600"><History className="size-4" /></span><div><p className="text-sm font-semibold text-[#153553]">{entry.action}</p><p className="mt-1 text-xs text-muted-foreground">{entry.description} · <span className="mono-number">{entry.date}</span></p></div></li>)}</ol>; }
